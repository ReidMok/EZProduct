# 🚀 EZProduct - 立即执行的命令

## 📋 当前状态

- ✅ 项目已创建
- ✅ 依赖已安装（npm install完成）
- ⏳ **接下来需要执行以下步骤**

## 🎯 在终端中依次执行（复制粘贴）

### 第一步：进入项目目录
```bash
cd /Users/Zhuanz/cursor/resinmemory_product/ezproduct
```

### 第二步：生成Prisma数据库客户端
```bash
npm run db:generate
```
**预期输出**：会显示 "Generated Prisma Client" 等成功信息

### 第三步：创建数据库表
```bash
npm run db:migrate
```
**提示**：如果询问迁移名称，直接按回车使用默认名称

### 第四步：创建.env文件
```bash
cp .env.example .env
```

### 第五步：编辑.env文件（重要！）
使用你喜欢的文本编辑器打开 `.env` 文件：

```bash
# macOS 使用
open -e .env

# 或使用 VS Code
code .env

# 或使用 nano
nano .env
```

然后填入以下内容（替换占位符）：

```env
# Shopify App Configuration
SHOPIFY_API_KEY=你的Shopify_API_Key
SHOPIFY_API_SECRET=你的Shopify_API_Secret
SCOPES=write_products,read_products,write_product_listings,read_product_listings
SHOPIFY_APP_URL=https://localhost:3000

# Database
DATABASE_URL="file:./dev.db"

# AI API Configuration
GEMINI_API_KEY=你的Gemini_API_Key

# App Environment
NODE_ENV=development
```

### 第六步：更新shopify.app.toml
编辑 `shopify.app.toml` 文件，更新 `client_id`：

```bash
# 使用文本编辑器打开
open -e shopify.app.toml
# 或
code shopify.app.toml
```

找到这一行：
```toml
client_id = "YOUR_CLIENT_ID"
```

替换为：
```toml
client_id = "你的Client_ID"  # 从Shopify Partners获取
```

### 第七步：启动开发服务器
```bash
npm run dev
```

**预期行为**：
- Shopify CLI 会自动启动
- 可能会打开浏览器让你选择开发商店
- 会显示一个临时的公网URL（用于Shopify访问）

## 🔑 获取API密钥（如果还没有）

### Google Gemini API Key
1. 访问：https://makersuite.google.com/app/apikey
2. 点击 "Create API Key"
3. 复制密钥到 `.env` 的 `GEMINI_API_KEY`

### Shopify API凭证
1. 访问：https://partners.shopify.com/
2. 进入你的应用（如果没有，先创建应用）
3. 在 "API credentials" 页面获取：
   - **Client ID** → 填入 `shopify.app.toml` 的 `client_id`
   - **API Secret** → 填入 `.env` 的 `SHOPIFY_API_SECRET`
   - **API Key** → 填入 `.env` 的 `SHOPIFY_API_KEY`

## ✅ 验证命令

每步完成后可以验证：

```bash
# 验证第二步完成
ls node_modules/.prisma/client && echo "✅ Prisma客户端已生成"

# 验证第三步完成
ls prisma/dev.db && echo "✅ 数据库已创建"

# 验证第四步完成
ls .env && echo "✅ .env文件已创建"
```

## 🎯 一键复制（完整命令序列）

```bash
# 进入项目目录
cd /Users/Zhuanz/cursor/resinmemory_product/ezproduct

# 生成Prisma客户端
npm run db:generate

# 创建数据库
npm run db:migrate

# 创建.env文件
cp .env.example .env

# 打开.env文件编辑（macOS）
open -e .env

# 提示
echo "✅ 请编辑 .env 文件填入API密钥"
echo "✅ 请编辑 shopify.app.toml 填入 client_id"
echo "✅ 完成后运行: npm run dev"
```

## ⚠️ 重要提示

1. **必须先执行第二步和第三步**，才能启动应用
2. **必须填入API密钥**，否则应用无法工作
3. **Shopify Client ID** 必须从Shopify Partners获取
4. **开发环境使用localhost**，Shopify CLI会自动处理网络穿透

## 🆘 如果遇到问题

### npm run db:generate 失败
- 检查是否已安装依赖：`ls node_modules`
- 如果失败，重新运行：`npm install`

### npm run db:migrate 失败
- 确保先执行了 `npm run db:generate`
- 检查 `prisma/schema.prisma` 文件是否存在

### .env文件找不到
- 确保执行了 `cp .env.example .env`
- 检查文件是否创建：`ls -la .env`

## 📝 下一步

完成以上步骤后：
1. ✅ 运行 `npm run dev` 启动应用
2. ✅ 在浏览器中选择开发商店
3. ✅ 测试应用功能




