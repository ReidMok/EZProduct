# 🔧 修复迁移历史问题

## ⚠️ 错误

```
Error: P3019
The datasource provider `postgresql` specified in your schema does not match the one specified in the migration_lock.toml, `sqlite`.
```

## 🔍 原因

之前开发时使用的是 SQLite，现在切换到 PostgreSQL，但迁移历史记录还是 SQLite 的。

## ✅ 解决方案

### 方案 1：删除旧迁移，重新创建（推荐）

#### 步骤 1：删除旧的迁移目录

```bash
cd /Users/Zhuanz/cursor/resinmemory_product/ezproduct

# 删除旧的迁移目录
rm -rf prisma/migrations
```

#### 步骤 2：创建新的 PostgreSQL 迁移

```bash
# 创建新的迁移
DATABASE_URL="postgresql://postgres.cugxiuizyhalmdxekged:Reiddavis1121...@aws-1-eu-west-1.pooler.supabase.com:5432/postgres" npx prisma migrate dev --name init
```

这会：
- 创建新的迁移目录
- 生成迁移 SQL
- 应用到数据库

#### 步骤 3：验证

迁移成功后，检查 Supabase：
- Table Editor 中应该能看到 `Shop` 和 `ProductGeneration` 表

---

### 方案 2：直接在 Supabase SQL Editor 中创建表（更简单）

如果方案 1 不行，直接在 Supabase 中手动创建表：

1. **打开 SQL Editor**
   - 在 Supabase Dashboard 中点击左侧 "SQL Editor"
   - 点击 "New query"

2. **运行以下 SQL**：

```sql
-- 创建 Shop 表
CREATE TABLE IF NOT EXISTS "Shop" (
  "id" TEXT NOT NULL,
  "shop" TEXT NOT NULL,
  "accessToken" TEXT NOT NULL,
  "scope" TEXT NOT NULL,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updatedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "Shop_pkey" PRIMARY KEY ("id")
);

CREATE UNIQUE INDEX IF NOT EXISTS "Shop_shop_key" ON "Shop"("shop");
CREATE INDEX IF NOT EXISTS "Shop_shop_idx" ON "Shop"("shop");

-- 创建 ProductGeneration 表
CREATE TABLE IF NOT EXISTS "ProductGeneration" (
  "id" TEXT NOT NULL,
  "shopId" TEXT NOT NULL,
  "keywords" TEXT NOT NULL,
  "imageUrl" TEXT,
  "title" TEXT NOT NULL,
  "descriptionHtml" TEXT NOT NULL,
  "tags" TEXT NOT NULL,
  "seoTitle" TEXT,
  "seoDescription" TEXT,
  "variantsJson" TEXT NOT NULL,
  "shopifyProductId" TEXT,
  "shopifyProductHandle" TEXT,
  "status" TEXT NOT NULL DEFAULT 'pending',
  "errorMessage" TEXT,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updatedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "ProductGeneration_pkey" PRIMARY KEY ("id")
);

CREATE INDEX IF NOT EXISTS "ProductGeneration_shopId_idx" ON "ProductGeneration"("shopId");
CREATE INDEX IF NOT EXISTS "ProductGeneration_status_idx" ON "ProductGeneration"("status");
CREATE INDEX IF NOT EXISTS "ProductGeneration_createdAt_idx" ON "ProductGeneration"("createdAt");

-- 添加外键约束
ALTER TABLE "ProductGeneration" 
  DROP CONSTRAINT IF EXISTS "ProductGeneration_shopId_fkey";

ALTER TABLE "ProductGeneration" 
  ADD CONSTRAINT "ProductGeneration_shopId_fkey" 
  FOREIGN KEY ("shopId") REFERENCES "Shop"("id") 
  ON DELETE CASCADE ON UPDATE CASCADE;
```

3. **点击 "Run" 执行**

---

## 🚀 推荐操作

**先尝试方案 1**（删除旧迁移，重新创建），如果不行再用方案 2（手动创建表）。

---

## ✅ 完成后的步骤

1. **更新 Vercel 环境变量**
   - Settings → Environment Variables
   - 更新 `DATABASE_URL` 为 Session Pooler 连接字符串

2. **验证表已创建**
   - 在 Supabase Table Editor 中检查表是否存在

3. **测试应用**
   - 在 Shopify 后台重新安装应用
   - 应该能正常保存会话




