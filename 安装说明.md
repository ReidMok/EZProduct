# EZProduct 安装说明

## 🔧 解决npm权限问题

你遇到的npm权限问题是因为系统级别的npm安装有权限限制。这不是给AI权限的问题，而是你的系统npm配置问题。

## 解决方案（按推荐顺序）

### 方案1：使用yarn（推荐，最简单）

如果你已经安装了yarn：

```bash
cd /Users/Zhuanz/cursor/resinmemory_product/ezproduct
yarn install
```

如果没有yarn，先安装：
```bash
npm install -g yarn
# 或者
brew install yarn
```

### 方案2：修复npm权限

```bash
# 方法A：使用sudo（不推荐，但可以快速解决）
cd /Users/Zhuanz/cursor/resinmemory_product/ezproduct
sudo npm install

# 方法B：修复npm全局目录权限（推荐）
mkdir ~/.npm-global
npm config set prefix '~/.npm-global'
echo 'export PATH=~/.npm-global/bin:$PATH' >> ~/.zshrc
source ~/.zshrc
npm install -g npm@latest
```

### 方案3：使用nvm（Node版本管理器）

```bash
# 安装nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 重启终端或运行
source ~/.zshrc

# 安装并使用Node.js
nvm install 18
nvm use 18

# 然后安装依赖
cd /Users/Zhuanz/cursor/resinmemory_product/ezproduct
npm install
```

### 方案4：使用pnpm（另一个包管理器）

```bash
# 安装pnpm
npm install -g pnpm

# 使用pnpm安装
cd /Users/Zhuanz/cursor/resinmemory_product/ezproduct
pnpm install
```

## 🚀 快速开始（推荐步骤）

1. **检查是否有yarn**：
   ```bash
   which yarn
   ```

2. **如果有yarn，直接使用**：
   ```bash
   cd /Users/Zhuanz/cursor/resinmemory_product/ezproduct
   yarn install
   yarn db:generate
   yarn db:migrate
   ```

3. **如果没有yarn，使用sudo**（快速但不优雅）：
   ```bash
   cd /Users/Zhuanz/cursor/resinmemory_product/ezproduct
   sudo npm install
   npm run db:generate
   npm run db:migrate
   ```

## 📝 安装后的步骤

无论使用哪种方法安装依赖，之后都需要：

1. **创建.env文件**：
   ```bash
   cp .env.example .env
   ```

2. **编辑.env文件**，填入：
   - GEMINI_API_KEY
   - SHOPIFY_API_KEY
   - SHOPIFY_API_SECRET
   - SHOPIFY_APP_URL

3. **初始化数据库**：
   ```bash
   npm run db:generate  # 或 yarn db:generate
   npm run db:migrate   # 或 yarn db:migrate
   ```

4. **启动开发服务器**：
   ```bash
   npm run dev  # 或 yarn dev
   ```

## 💡 建议

**最简单的方法**：使用yarn，它通常不会有权限问题。

如果yarn也没有，那就用sudo npm install（虽然不优雅，但能快速解决问题）。

