# 🚀 快速部署步骤（跳过本地测试）

## 📋 3 步完成部署

### 步骤1：创建 Supabase 数据库（5分钟）

1. **访问** https://supabase.com/dashboard
2. **创建新项目**
   - 点击 "New Project"
   - 填写项目名称和密码（保存密码！）
   - 等待创建完成（2-3分钟）

3. **获取数据库连接字符串**
   - 进入项目 → Settings → Database
   - 找到 "Connection string" → 选择 "URI"
   - 复制连接字符串
   - **重要**：将 `[YOUR-PASSWORD]` 替换为你设置的密码

### 步骤2：部署到 Vercel（10分钟）

1. **访问** https://vercel.com
2. **用 GitHub 登录**
3. **导入项目**
   - 点击 "Add New" → "Project"
   - 如果代码在 GitHub，选择仓库
   - 如果没有，先推送到 GitHub：
     ```bash
     cd /Users/Zhuanz/cursor/resinmemory_product/ezproduct
     git init
     git add .
     git commit -m "Initial commit"
     # 在 GitHub 创建仓库后：
     git remote add origin https://github.com/你的用户名/ezproduct.git
     git push -u origin main
     ```

4. **配置环境变量**
   在 Vercel 项目设置 → Environment Variables 中添加：

   ```
   SHOPIFY_API_KEY=你的Shopify_API_Key
   SHOPIFY_API_SECRET=你的Shopify_API_Secret
   SCOPES=write_products,read_products,write_product_listings,read_product_listings
   SHOPIFY_APP_URL=https://你的应用.vercel.app（部署后更新）
   DATABASE_URL=你的Supabase连接字符串
   GEMINI_API_KEY=你的Gemini_API_Key
   NODE_ENV=production
   ```

5. **部署**
   - 点击 "Deploy"
   - 等待 2-3 分钟
   - 复制部署后的 URL（例如：`https://ezproduct-xxx.vercel.app`）

### 步骤3：更新 Shopify 配置并安装应用（5分钟）

1. **更新 Shopify Partners Dashboard**
   - 访问 https://partners.shopify.com/
   - 进入你的应用 → "App setup"
   - 更新 **App URL** 为你的 Vercel URL
   - 更新 **Redirect URLs**：
     ```
     https://你的应用.vercel.app/auth/callback
     https://你的应用.vercel.app/auth/shopify/callback
     https://你的应用.vercel.app/api/auth/callback
     ```
   - 保存

2. **更新应用版本**
   - 进入 "Versions" → 你的版本
   - 更新 **App URL** 和 **Redirect URLs**
   - 保存并发布

3. **运行数据库迁移**
   ```bash
   cd /Users/Zhuanz/cursor/resinmemory_product/ezproduct
   DATABASE_URL="你的Supabase连接字符串" npm run db:migrate
   ```

4. **在 Shopify 后台安装应用**
   - 访问你的开发商店后台
   - 进入 "Apps" → 找到 "EZProduct"
   - 点击 "Install"
   - 完成授权

## ✅ 完成！

现在你的应用已经在生产环境运行了！

## 🎯 下一步

1. **测试应用功能**
   - 打开应用界面
   - 输入产品关键词
   - 测试产品生成

2. **查看日志**
   - Vercel Dashboard → 你的项目 → "Logs"
   - 查看应用运行日志

3. **更新代码**
   - 推送到 GitHub
   - Vercel 会自动重新部署

## 💰 成本

- **Vercel**: 免费（100GB/月带宽）
- **Supabase**: 免费（500MB 数据库）
- **总计**: $0/月（起步阶段）

## 🆘 遇到问题？

### 数据库连接失败
- 检查 Supabase 连接字符串是否正确
- 确保密码已替换 `[YOUR-PASSWORD]`

### 部署失败
- 检查环境变量是否都已设置
- 查看 Vercel 日志找出错误

### OAuth 安装失败
- 确保 Shopify Partners Dashboard 中的 URL 与 Vercel URL 一致
- 等待几分钟让配置生效




