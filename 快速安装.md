# EZProduct - 快速安装命令

## 🚀 一键安装（复制粘贴执行）

在终端中依次执行以下命令：

### 1. 进入项目目录
```bash
cd /Users/Zhuanz/cursor/resinmemory_product/ezproduct
```

### 2. 安装依赖包
```bash
npm install
```
如果遇到权限问题，使用：
```bash
sudo npm install
```

### 3. 生成Prisma客户端
```bash
npm run db:generate
```

### 4. 创建数据库
```bash
npm run db:migrate
```

### 5. 创建环境变量文件
```bash
cp .env.example .env
```

### 6. 编辑.env文件（重要！）
使用你喜欢的编辑器打开 `.env` 文件，填入以下信息：

```env
# Shopify App Configuration
SHOPIFY_API_KEY=你的Shopify_API_Key
SHOPIFY_API_SECRET=你的Shopify_API_Secret
SCOPES=write_products,read_products,write_product_listings,read_product_listings
SHOPIFY_APP_URL=https://你的应用URL

# Database
DATABASE_URL="file:./dev.db"

# AI API Configuration
GEMINI_API_KEY=你的Gemini_API_Key

# App Environment
NODE_ENV=development
```

### 7. 启动开发服务器
```bash
npm run dev
```

## 📋 完整命令序列（复制粘贴）

```bash
# 进入项目目录
cd /Users/Zhuanz/cursor/resinmemory_product/ezproduct

# 安装依赖
npm install

# 初始化数据库
npm run db:generate
npm run db:migrate

# 创建环境变量文件
cp .env.example .env

# 提示：现在需要编辑.env文件填入API密钥
echo "请编辑 .env 文件，填入你的API密钥"

# 启动开发服务器（填入API密钥后执行）
# npm run dev
```

## 🔑 获取API密钥

### Google Gemini API Key
1. 访问：https://makersuite.google.com/app/apikey
2. 点击"Create API Key"
3. 复制密钥到 `.env` 文件的 `GEMINI_API_KEY`

### Shopify API凭证
1. 访问：https://partners.shopify.com/
2. 登录你的Partner账户
3. 创建新应用或选择现有应用
4. 在"API credentials"中获取：
   - API Key → `SHOPIFY_API_KEY`
   - API Secret → `SHOPIFY_API_SECRET`

## ✅ 安装完成检查

执行以下命令检查是否安装成功：

```bash
# 检查node_modules是否存在
ls node_modules | head -5

# 检查数据库文件是否创建
ls prisma/dev.db

# 检查.env文件是否存在
ls .env
```

如果以上文件都存在，说明安装成功！

## 🎯 下一步

1. ✅ 安装依赖完成
2. ✅ 数据库初始化完成
3. ⏳ 编辑 `.env` 文件填入API密钥
4. ⏳ 更新 `shopify.app.toml` 配置
5. ⏳ 运行 `npm run dev` 启动应用




