# EZProduct - 完成配置步骤

## 📋 当前进度

- [x] ✅ 项目已创建
- [x] ✅ 依赖已安装（npm install）
- [ ] ⏳ 生成Prisma客户端
- [ ] ⏳ 创建数据库
- [ ] ⏳ 创建.env文件
- [ ] ⏳ 配置API密钥
- [ ] ⏳ 启动开发服务器

## 🚀 接下来执行的命令（按顺序）

### 步骤1：生成Prisma客户端
```bash
cd /Users/Zhuanz/cursor/resinmemory_product/ezproduct
npm run db:generate
```

### 步骤2：创建数据库表
```bash
npm run db:migrate
```
提示输入迁移名称时，直接按回车使用默认名称。

### 步骤3：创建.env文件
```bash
cp .env.example .env
```

### 步骤4：编辑.env文件
使用文本编辑器打开 `.env` 文件，填入以下内容：

```env
# Shopify App Configuration
SHOPIFY_API_KEY=你的Shopify_API_Key
SHOPIFY_API_SECRET=你的Shopify_API_Secret
SCOPES=write_products,read_products,write_product_listings,read_product_listings
SHOPIFY_APP_URL=https://localhost:3000

# Database
DATABASE_URL="file:./dev.db"

# AI API Configuration
GEMINI_API_KEY=你的Gemini_API_Key

# App Environment
NODE_ENV=development
```

### 步骤5：更新shopify.app.toml
编辑 `shopify.app.toml` 文件，更新：
```toml
client_id = "你的Client_ID"  # 从Shopify Partners获取
```

### 步骤6：启动开发服务器
```bash
npm run dev
```

## 🔑 获取API密钥

### Google Gemini API Key
1. 访问：https://makersuite.google.com/app/apikey
2. 点击 "Create API Key"
3. 复制密钥

### Shopify API凭证
1. 访问：https://partners.shopify.com/
2. 进入你的应用
3. 在 "API credentials" 获取：
   - Client ID → `shopify.app.toml` 的 `client_id`
   - API Secret → `.env` 的 `SHOPIFY_API_SECRET`
   - API Key → `.env` 的 `SHOPIFY_API_KEY`

## ✅ 验证步骤

每步完成后验证：

```bash
# 验证步骤1完成
ls node_modules/.prisma/client && echo "✅ Prisma客户端已生成"

# 验证步骤2完成
ls prisma/dev.db && echo "✅ 数据库已创建"

# 验证步骤3完成
ls .env && echo "✅ .env文件已创建"
```

## 🎯 一键执行（复制粘贴）

```bash
# 进入项目目录
cd /Users/Zhuanz/cursor/resinmemory_product/ezproduct

# 步骤1：生成Prisma客户端
npm run db:generate

# 步骤2：创建数据库
npm run db:migrate

# 步骤3：创建.env文件
cp .env.example .env

# 提示
echo "✅ 请编辑 .env 文件填入API密钥，然后运行: npm run dev"
```

