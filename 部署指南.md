# EZProduct - 部署到生产环境指南

## 🚀 部署方案：Vercel + Supabase（推荐）

### 第一步：准备 Supabase 数据库

1. **注册 Supabase**
   - 访问：https://supabase.com
   - 点击 "Start your project"
   - 用 GitHub 账号登录（推荐）

2. **创建新项目**
   - 点击 "New Project"
   - 填写项目信息：
     - **Name**: `ezproduct` 或你喜欢的名称
     - **Database Password**: 设置一个强密码（保存好！）
     - **Region**: 选择离你最近的区域
   - 点击 "Create new project"
   - 等待 2-3 分钟项目创建完成

3. **获取数据库连接字符串**
   - 进入项目后，点击左侧 "Settings"
   - 点击 "Database"
   - 找到 "Connection string"
   - 选择 "URI" 标签
   - 复制连接字符串（格式：`postgresql://postgres:[YOUR-PASSWORD]@...`）
   - **重要**：将 `[YOUR-PASSWORD]` 替换为你创建项目时设置的密码

### 第二步：准备 Vercel 部署

1. **注册 Vercel**
   - 访问：https://vercel.com
   - 点击 "Sign Up"
   - 用 GitHub 账号登录（推荐）

2. **准备 GitHub 仓库**
   - 在 GitHub 创建新仓库
   - 将项目代码推送到仓库：
     ```bash
     git init
     git add .
     git commit -m "Initial commit"
     git remote add origin https://github.com/你的用户名/ezproduct.git
     git push -u origin main
     ```

### 第三步：部署到 Vercel

1. **导入项目**
   - 在 Vercel Dashboard 点击 "Add New" → "Project"
   - 选择你的 GitHub 仓库
   - 点击 "Import"

2. **配置项目设置**
   - **Framework Preset**: Remix（自动检测）
   - **Root Directory**: `./`（默认）
   - **Build Command**: `npm run build`（默认）
   - **Output Directory**: `.vercel`（默认）

3. **配置环境变量**
   在 "Environment Variables" 中添加：

   ```env
   # Shopify
   SHOPIFY_API_KEY=你的Shopify_API_Key
   SHOPIFY_API_SECRET=你的Shopify_API_Secret
   SCOPES=write_products,read_products,write_product_listings,read_product_listings
   SHOPIFY_APP_URL=https://你的vercel域名.vercel.app

   # Database (Supabase)
   DATABASE_URL=postgresql://postgres:你的密码@db.xxx.supabase.co:5432/postgres

   # AI
   GEMINI_API_KEY=你的Gemini_API_Key

   # Environment
   NODE_ENV=production
   ```

4. **部署**
   - 点击 "Deploy"
   - 等待 2-3 分钟部署完成
   - 复制部署后的 URL（例如：`https://ezproduct.vercel.app`）

### 第四步：更新数据库 Schema

1. **更新 Prisma Schema**
   - 编辑 `prisma/schema.prisma`
   - 将 `provider` 改为 `postgresql`：
     ```prisma
     datasource db {
       provider = "postgresql"
       url      = env("DATABASE_URL")
     }
     ```

2. **运行迁移**
   ```bash
   # 在本地运行（使用 Supabase 的连接字符串）
   DATABASE_URL="你的Supabase连接字符串" npm run db:migrate
   ```

   或者在 Vercel 部署后，通过 Vercel CLI 运行：
   ```bash
   vercel env pull .env.local
   npm run db:migrate
   ```

### 第五步：更新 Shopify 配置

1. **更新 Shopify Partners Dashboard**
   - 进入你的应用设置
   - 更新 **App URL** 为你的 Vercel URL
   - 更新 **Allowed redirection URL(s)**：
     ```
     https://你的vercel域名.vercel.app/auth/callback
     https://你的vercel域名.vercel.app/auth/shopify/callback
     https://你的vercel域名.vercel.app/api/auth/callback
     ```

2. **更新 `shopify.app.toml`**
   ```toml
   application_url = "https://你的vercel域名.vercel.app"
   ```

### 第六步：测试生产环境

1. **访问你的应用**
   - 打开 Vercel 提供的 URL
   - 测试应用功能

2. **在 Shopify 中安装**
   - 在开发商店中安装应用
   - 测试完整流程

## 💰 成本估算

### 免费额度（足够起步）

- **Vercel**: 
  - 100GB 带宽/月
  - 1000 小时运行时间/月
  - **完全免费**

- **Supabase**:
  - 500MB 数据库
  - 2GB 带宽/月
  - **完全免费**

### 如果用户增长

- **Vercel Pro**: $20/月（如果需要更多资源）
- **Supabase Pro**: $25/月（如果需要更多数据库空间）
- **AI API**: 按使用量（每次生成约 $0.01-0.05）

## ✅ 部署检查清单

- [ ] 注册 Supabase 并创建项目
- [ ] 获取 Supabase 数据库连接字符串
- [ ] 注册 Vercel 账号
- [ ] 将代码推送到 GitHub
- [ ] 在 Vercel 中导入项目
- [ ] 配置所有环境变量
- [ ] 更新 Prisma Schema 为 PostgreSQL
- [ ] 运行数据库迁移
- [ ] 更新 Shopify 应用 URL
- [ ] 测试生产环境

## 🆘 常见问题

### Q: 部署后数据库连接失败？
A: 检查：
- Supabase 连接字符串是否正确
- 密码是否已替换 `[YOUR-PASSWORD]`
- Vercel 环境变量是否已设置

### Q: 如何查看 Vercel 日志？
A: 在 Vercel Dashboard → 你的项目 → "Logs" 标签

### Q: 如何更新代码？
A: 推送到 GitHub，Vercel 会自动重新部署

### Q: 数据库迁移失败？
A: 确保：
- Prisma Schema 已更新为 `postgresql`
- 连接字符串正确
- Supabase 项目已创建完成

## 📝 总结

**现在（开发阶段）**：
- ✅ 使用本地 SQLite（免费）
- ✅ 本地运行（免费）
- ✅ 不需要任何云服务

**稍后（生产阶段）**：
- ✅ Vercel 部署（免费额度很大）
- ✅ Supabase 数据库（免费额度很大）
- ✅ 几乎零成本起步




